import Flex from "../../components/Flex"

export default ({...props})=>{
    const classes="mt-[10px] px-[10px] "
    return(
        <Flex  {...props} className={classes}>
            
            </Flex>
    )
}